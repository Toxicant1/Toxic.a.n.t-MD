const { startWeb } = require("./pairing");

// Start the clean pairing interface
// This handles the web UI and the Baileys pairing logic
console.log("SYSTEM: Initializing Toxic.a.n.t-MD Web Service...");
startWeb();
