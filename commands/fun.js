module.exports = {

    // =======
    fact: async (msg, args, bot) => {
        return `🎭 *TOXIC.a.n.t MD* | Did you know? ${args.join(" ")}`;
    },

    // =======
    funfact: async (msg, args, bot) => {
        return `🎭 *TOXIC.a.n.t MD* | Fun fact: ${args.join(" ")}`;
    },

    // =======
    catfact: async (msg, args, bot) => {
        return `🐱 *TOXIC.a.n.t MD* | Cat fact: ${args.join(" ")}`;
    },

    // =======
    advice: async (msg, args, bot) => {
        return `💡 *TOXIC.a.n.t MD* | Advice: ${args.join(" ")}`;
    },

    // =======
    joke: async (msg, args, bot) => {
        return `😂 *TOXIC.a.n.t MD* | Joke: ${args.join(" ")}`;
    },

    // =======
    news: async (msg, args, bot) => {
        return `📰 *TOXIC.a.n.t MD* | Latest news: ${args.join(" ")}`;
    },

    // =======
    rship: async (msg, args, bot) => {
        return `❤️ *TOXIC.a.n.t MD* | Relationship advice: ${args.join(" ")}`;
    },

    // =======
    gpass: async (msg, args, bot) => {
        return `🔑 *TOXIC.a.n.t MD* | Generated password: ${args.join(" ")}`;
    },

    // =======
    anime: async (msg, args, bot) => {
        return `🌸 *TOXIC.a.n.t MD* | Anime info: ${args.join(" ")}`;
    },

    // =======
    animegirl: async (msg, args, bot) => {
        return `👧 *TOXIC.a.n.t MD* | Anime girl info: ${args.join(" ")}`;
    },

    // =======
    quotes: async (msg, args, bot) => {
        return `✍️ *TOXIC.a.n.t MD* | Quote: ${args.join(" ")}`;
    },

    // =======
    pickupline: async (msg, args, bot) => {
        return `💌 *TOXIC.a.n.t MD* | Pickup line: ${args.join(" ")}`;
    }

};